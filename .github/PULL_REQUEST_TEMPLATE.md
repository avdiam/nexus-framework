<!-- Version: 1.0.0 | Sprint: 112 | ISS-100 step 2.1 -->

<!-- Please read CONTRIBUTING.md first. In short: this repository is a generated export.
     Pull requests are read, reviewed and answered, and accepted changes are ported upstream
     and credited in the release notes rather than merged here. Nothing is lost — but a commit
     landing directly on this branch would be, at the next export. -->

## What this changes

<!-- One change per pull request. -->

## Why

<!-- The problem it solves. Link the issue if there is one. -->

## Files touched

| File | Change | Version bumped |
|---|---|---|
| | | |

<!-- Framework files carry a `*Version: X.Y.Z*` header: major for structural section changes,
     minor for new or changed rules, patch for wording. -->

## Fresh-install run

**This is the part that matters most.** Please test on a real installation rather than on this
repository — the two are not the same tree, and several defects have only ever appeared on the
installed side.

- Install route used: <!-- plugin / clone -->
- [ ] `start` → `/nexus-init-project` → `/nexus-setup-project` → `/nexus-generate-mvp` →
      `/nexus-organize-sprint` all completed
- [ ] I exercised the operation I changed, and one that consumes it

**Startup header from that run** — verbatim, including any `⚠` line. A change that adds a warning
to a first boot is a regression even if the operation itself works:

```

```

## Checklist

- [ ] One change, one pull request
- [ ] Routing and menu entries updated, if this adds or renames an operation
- [ ] Guides updated, if this changes behavior a guide describes
- [ ] I offer this under the repository's MIT license
