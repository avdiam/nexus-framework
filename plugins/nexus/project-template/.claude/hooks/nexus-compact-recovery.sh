#!/bin/bash
# SessionStart hook (compact matcher) — re-boot instruction after compaction
echo "⚠️ NEXUS: Context was compacted."
echo ""
echo "MANDATORY: Invoke /nexus-start to re-initialize the framework."
echo "(CLAUDE.md is still in context — the skill will detect your session mode.)"
